package ssl.tests;

import org.junit.Test;

public class NodeTest
{

    @Test
    public void testNode()
    {
        /*
         * Node n = new Node( "main" )
         * .addAfter( new Node("stringspace"))
         * .addBefore( new Node("proctable"));
         * 
         * Node<String> k = new Node("proc1")
         * .addBefore(n);
         * 
         * k.pushBack(new Node("proc2"));
         * n.pushFront(new Node("head"));
         * 
         * Iterator<String> i = k.getHead().iterator();
         * 
         * assertEquals(i.next(), "head");
         * assertEquals(i.next(), "main");
         * assertEquals(i.next(), "proctable");
         * assertEquals(i.next(), "stringspace");
         * assertEquals(i.next(), "proc1");
         * assertEquals(i.next(), "proc2");
         * assertEquals(i.hasNext(), false);
         */
    }
}
